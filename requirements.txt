# Direct dependencies of realtime_transcribe.py
PyAudio==0.2.14
faster-whisper==1.2.1
numpy==2.5.0
python-docx==1.2.0

# Transitive dependencies (faster-whisper)
av==17.1.0
ctranslate2==4.8.0
onnxruntime==1.27.0
tokenizers==0.23.1
tqdm==4.68.3

# Transitive dependencies (huggingface_hub via faster-whisper)
huggingface_hub==1.21.0
filelock==3.29.4
fsspec==2026.6.0
hf-xet==1.5.1
httpx==0.28.1
packaging==26.2
PyYAML==6.0.3
typer==0.25.1
typing_extensions==4.15.0

# Transitive dependencies (httpx)
anyio==4.14.1
certifi==2026.6.17
httpcore==1.0.9
h11==0.16.0
idna==3.18

# Transitive dependencies (requests via huggingface_hub)
requests==2.34.2
charset-normalizer==3.4.7
urllib3==2.7.0

# Transitive dependencies (onnxruntime)
flatbuffers==25.12.19
protobuf==7.35.1

# Transitive dependencies (typer)
annotated-doc==0.0.4
click==8.4.2
rich==15.0.0
shellingham==1.5.4

# Transitive dependencies (rich)
markdown-it-py==4.2.0
mdurl==0.1.2
Pygments==2.20.0

# Transitive dependencies (python-docx)
lxml==6.1.1

# Transitive dependencies (tqdm / click on Windows)
colorama==0.4.6

# Transitive dependencies (ctranslate2)
setuptools==82.0.1
