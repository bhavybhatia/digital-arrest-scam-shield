# Direct dependencies
PyAudio==0.2.14
openai-whisper==20250625
numpy==2.4.6
python-docx==1.2.0

# Transitive dependencies (openai-whisper)
more-itertools==11.1.0
numba==0.65.1
tiktoken==0.13.0
torch==2.12.1
tqdm==4.68.3

# Transitive dependencies (numba)
llvmlite==0.47.0

# Transitive dependencies (tiktoken)
regex==2026.6.28
requests==2.34.2

# Transitive dependencies (requests)
certifi==2026.6.17
charset-normalizer==3.4.7
idna==3.18
urllib3==2.7.0

# Transitive dependencies (torch)
filelock==3.29.4
fsspec==2026.6.0
jinja2==3.1.6
networkx==3.6.1
setuptools==81.0.0
sympy==1.14.0
typing_extensions==4.15.0

# Transitive dependencies (sympy)
mpmath==1.3.0

# Transitive dependencies (jinja2)
MarkupSafe==3.0.3

# Transitive dependencies (tqdm)
colorama==0.4.6

# Transitive dependencies (python-docx)
lxml==6.1.1
